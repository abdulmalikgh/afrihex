# Unit Tests

Install when test work begins:

```bash
npx expo install jest-expo jest --dev
npx expo install @testing-library/react-native --dev
```

Expected command:

```bash
npm test
```

Initial coverage:

- API client
- auth storage
- form validation
- screen state rendering
- coordinate conversion
