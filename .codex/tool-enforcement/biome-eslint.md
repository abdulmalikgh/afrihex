# Biome / ESLint

No formatter/linter is configured yet.

Preferred direction:

- Add one formatter/linter stack only.
- Keep rules pragmatic for React Native and Expo.
- Avoid style rules that conflict with Prettier-like formatting.

Recommended future option:

```txt
Biome for formatting and linting
```

Alternative:

```txt
ESLint with Expo/React Native rules plus Prettier
```

Do not introduce both Biome and ESLint unless there is a clear split of responsibility.
