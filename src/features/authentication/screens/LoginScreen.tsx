import { PlaceholderScreen } from '../../../screens/PlaceholderScreen';

export function LoginScreen() {
  return (
    <PlaceholderScreen
      eyebrow="Login"
      title="Access your AfriHex account"
      description="Email/password auth, Google exchange, token storage, and current user loading will live here."
      primaryAction="Continue"
    />
  );
}
