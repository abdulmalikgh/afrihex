import { PlaceholderScreen } from '../../../screens/PlaceholderScreen';

export function RegisterScreen() {
  return (
    <PlaceholderScreen
      eyebrow="Register"
      title="Create an AfriHex account"
      description="Name, email, password, token storage, and first usage profile fetch will live here."
      primaryAction="Create account"
    />
  );
}
