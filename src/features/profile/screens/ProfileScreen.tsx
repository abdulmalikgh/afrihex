import { PlaceholderScreen } from '../../../screens/PlaceholderScreen';

export function ProfileScreen() {
  return (
    <PlaceholderScreen
      eyebrow="Profile"
      title="Account, usage, and address links"
      description="Login state, daily usage, API key status, and saved profile links will live here."
      primaryAction="Create address link"
    />
  );
}
