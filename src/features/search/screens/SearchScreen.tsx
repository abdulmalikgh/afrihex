import { PlaceholderScreen } from '../../../screens/PlaceholderScreen';

export function SearchScreen() {
  return (
    <PlaceholderScreen
      eyebrow="Find"
      title="Search Ghana by place, GPS name, or address"
      description="Autocomplete, reverse lookup, nearby places, and structured address parsing will live here."
      code="GL-0563-7842"
      primaryAction="Use current location"
    />
  );
}
