import { PlaceholderScreen } from '../../../screens/PlaceholderScreen';

export function MapScreen() {
  return (
    <PlaceholderScreen
      eyebrow="Map"
      title="Explore AfriHex locations on a map"
      description="MapLibre rendering, result markers, route polylines, hazard overlays, and selected-location sheets will live here."
      code="AF-GH"
      primaryAction="Open lookup"
    />
  );
}
